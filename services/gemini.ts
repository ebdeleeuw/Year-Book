// Gemini integration removed.
export const generateReflection = async (text: string): Promise<string> => {
    return "";
};

export const generateVisualization = async (text: string): Promise<string | null> => {
    return null;
}